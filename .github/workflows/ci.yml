name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      - run: uv sync --frozen --extra dev
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      # basedpyright resolves third-party imports from .venv (see the venv keys
      # in [tool.basedpyright]); --extra ui covers duckdb so every in-scope
      # services module's imports resolve. Scope + rules live in pyproject.toml.
      - run: uv sync --frozen --extra dev --extra ui
      # Scoped to services/ + pure-logic modules; fails on any NEW type error.
      - run: uv run basedpyright

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      # --extra ui installs streamlit/altair/plotly/duckdb so test_page_imports.py
      # (imports every Streamlit page) can collect and run in CI rather than erroring.
      - run: uv sync --frozen --extra dev --extra ui
      # Skip markers that can't run in CI:
      #   integration / sql / bronze — need committed pipeline output, but
      #     data/gold is gitignored, so these are LOCAL-ONLY for now. Run them
      #     after a pipeline run with:  uv run pytest -m "integration or sql or bronze"
      #     (set DAIL_INTEGRATION_TESTS=1 for the view tests lacking fixtures).
      #     They become CI-runnable once outputs are fixtured/built — see
      #     doc/CI_CD.md Phase 6.
      #   sources — external network. Source-URL health runs on a schedule via
      #     .github/workflows/nightly.yml (pdf_endpoint_check.py), not here.
      - run: uv run pytest -m "not integration and not sql and not sources and not bronze" -v
