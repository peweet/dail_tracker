{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Oireachtas /v1/debates response envelope",
  "description": "Loose validation: catches a renamed top-level key (results -> data), a dropped head.counts.resultCount, or a renamed result wrapper (debateRecord -> ...). Permissive on the nested fields the API may add over time. See services/schema_validation.py.",
  "type": "object",
  "required": ["head", "results"],
  "properties": {
    "head": {
      "type": "object",
      "required": ["counts"],
      "properties": {
        "counts": {
          "type": "object",
          "required": ["resultCount"],
          "properties": {
            "resultCount": { "type": "integer" }
          }
        }
      }
    },
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["debateRecord"],
        "properties": {
          "debateRecord": { "type": "object" }
        }
      }
    }
  }
}
