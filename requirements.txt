# pip / non-uv FALLBACK ONLY — this is NOT what Streamlit Community Cloud installs.
#
# Cloud installs from uv.lock (the highest-priority dependency file it finds) by
# running `uv sync`, which installs the core [project.dependencies] in
# pyproject.toml — i.e. the app runtime. See:
#   https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/app-dependencies
#
# This file exists solely so the app can also be run via plain
# `pip install -r requirements.txt` by anyone not using uv. It mirrors the
# app-runtime specifiers in pyproject's core [project.dependencies].
# test/test_requirements_sync.py fails CI if these drift from pyproject.
#
# pandas held <3.0: pandas 3.0 + plotly 6.7 has an import-order pd.Series crash.

streamlit>=1.50,<2
altair>=6.0,<7
plotly>=6.0,<7
duckdb>=1.4,<2
pandas>=2.2,<3.0
pyarrow>=14
