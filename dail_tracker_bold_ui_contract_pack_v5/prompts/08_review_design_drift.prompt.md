Review page `<PAGE_ID>` for design drift.

The page should not drift into:
- generic Streamlit defaults
- fintech SaaS dashboard
- AI gradient dashboard
- decorative chart wall
- page-local CSS system
- copied visual design from external sites

It should preserve:
- Dáil Tracker civic/editorial theme
- shared CSS
- reusable UI helpers
- evidence-first layout

Result contract:
- `Verdict: PASS | FAIL` (`PASS` requires no blocker or major finding)
- each finding with `Severity`, concrete `Evidence`, user consequence, and required action
- checks or screenshots used, plus residual risk
