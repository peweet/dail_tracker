Apply the Votes explorer flow.

Use external vote explorers only as flow inspiration:
search/list -> vote detail -> result summary -> member/group filters -> official source links -> CSV export.

Do not copy external visual design, branding, typography, colours, or CSS.

Keep Dáil Tracker's civic/editorial theme.
Use shared CSS only.

Missing vote index/detail/source data becomes:
`TODO_PIPELINE_VIEW_REQUIRED`.
