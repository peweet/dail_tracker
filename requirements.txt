# Streamlit Cloud deploy — UI runtime only.
#
# Pipeline deps (polars, PyMuPDF, beautifulsoup4, flatten-json) live in
# pyproject.toml and are NOT installed on cloud since the app reads pre-built
# parquet from data/gold/ and data/silver/, not raw PDFs or API JSON.
#
# Bounds rationale:
#   - Upper bounds catch breaking-major releases (Altair 5→6 dropped APIs;
#     Plotly 5→6 dropped Python 3.8). Loosen only after end-to-end testing.
#   - pandas capped <3.0 for launch: pandas 3.0 + plotly 6.7 has an import-order
#     pd.Series bug (mirrors the cap in pyproject.toml core deps). Revisit
#     post-launch and lift both together.
#
# Keep this file in sync with pyproject.toml (core deps + [ui] extra). The
# test/test_requirements_sync.py guard fails CI if these specifiers drift.

streamlit>=1.50,<2
pandas>=2.2,<3.0
duckdb>=1.4,<2
altair>=6.0,<7
plotly>=6.0,<7
pyarrow>=14
